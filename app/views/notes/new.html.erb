<h1>Create New Note</h1>

<%= form_for(@note, url: appointments_path)  do |f| %>

  <div class="field">
    <%= f.label :Cause %><br />
    <%= f.text_area :description %>
  </div>

  <div class = "button">
    <%= f.submit "Add Note" %>
  </div>

<% end %>
